# `@opentag/control-protocol`

Canonical runtime schemas, types, and digest helpers for OpenTag Control V1.

`@opentag/core` re-exports this package for compatibility. New Control Plane
transport code should import this focused package directly.
